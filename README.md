## download the project in zip and extract, open the folder and from cmd in this folder<br/>
run mvn clean install in cmd <br/>
open in IDE and run or mvn run builld in cmd <br/>
need maven and java <br/>
